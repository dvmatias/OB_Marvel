package com.cmdv.ph_home.ui.adapters

/**
 * Enumeration. This class holds all the view types in [CharacterAdapter] adapter.
 */
enum class CharacterAdapterViewType(val viewType: Int) {
    HEADER_MAIN(0),
    HEADER_INDEX(1),
    CHARACTER(2),
    FOOTER_LOADING(3)
}